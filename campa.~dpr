program campa;

uses
  Forms,
  Un_principal in 'Un_principal.pas' {FPrincipal},
  Un_Config in 'Un_Config.pas' {FConfig},
  Un_Sobre in 'Un_Sobre.pas' {FSobre};

{$R *.res}

begin
  Application.Initialize;
  Application.Title := 'Campa Automatizada';
  Application.CreateForm(TFPrincipal, FPrincipal);
  Application.CreateForm(TFConfig, FConfig);
  Application.CreateForm(TFSobre, FSobre);
  Application.Run;
end.
